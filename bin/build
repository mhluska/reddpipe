#!/bin/bash

call_dir=$(pwd)
root_dir=$(dirname $(dirname ${0}))

cd ${root_dir}

install -m 764 bin/pre-commit .git/hooks

git submodule update --init --recursive

cd sub/zepto
bundle
rake
cd -

npm install

coffee -c grunt.coffee
grunt

cd ${call_dir}
